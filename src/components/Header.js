const Header = () => {
  return (
    <header className="header">
      <h1>Task Tracker</h1>
      <button
        className="btn"
        style={{ backgroundColor: "green", color: "white" }}
      >
        Add
      </button>
    </header>
  );
};

export default Header;
